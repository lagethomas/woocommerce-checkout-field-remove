/**
 * WooCommerce Checkout Field Remover Admin JS
 * Usando Vanilla JS (atualmente sem funcionalidade necessária)
 * * @since 1.0.0
 */

document.addEventListener('DOMContentLoaded', function() {
    // Código Vanilla JS para interação futura, se necessário.
    // Exemplo:
    // const switches = document.querySelectorAll('.wcrf-switch input[type="checkbox"]');
    // switches.forEach(function(s) {
    //     s.addEventListener('change', function() {
    //         console.log(this.id + ' alterado para: ' + this.checked);
    //     });
    // });
});